mixin login()
    .card.card_login.container.container_direction-column
        h1.h1 Войти
        div
            +textFiled({})
            +textFiled({placeholder: 'Пароль'})
        +bigButton({sign: 'Войти'})
        +haveAcc()
